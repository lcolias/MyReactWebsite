import {render} from "@testing-library/react"
import React from "react";
import Funstuff from "./Funstuff"
/*
test("renders an h1", () => {
    const { getByText } = render(<Funstuff />);
    const h1 = getByText(/Fun-Stuff Page!/);
    expect(h1).toHaveTextContent("Fun-Stuff Page!");
}) */